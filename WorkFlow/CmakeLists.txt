set(WORKFLOW_ENGINE_ROOT_BUILD_DIR "${GLOBAL_OUTPUT_DIR}/Altera-Ody-Engine")

if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    set(ENGINE_WF_PREFIX "${WORKFLOW_ENGINE_ROOT_BUILD_DIR}/Debug")
else()
    set(ENGINE_WF_PREFIX "${WORKFLOW_ENGINE_ROOT_BUILD_DIR}/Release")
endif()

set(WORKFLOW_APP_ROOT_BUILD_DIR "${GLOBAL_OUTPUT_DIR}/Altera-Ody-App")

if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    set(APP_WF_PREFIX "${WORKFLOW_APP_ROOT_BUILD_DIR}/Debug")
else()
    set(APP_WF_PREFIX "${WORKFLOW_APP_ROOT_BUILD_DIR}/Release")
endif()

# ------------------------
# Add subdirectories
# ------------------------

add_subdirectory(App)
add_subdirectory(Engine) 